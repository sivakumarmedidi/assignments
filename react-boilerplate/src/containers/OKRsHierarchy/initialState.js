import * as Immutable from "seamless-immutable";

export default Immutable.from({
	isLoadingOKRs: true,
	selectedCategory: "",
	applicableCategoryFilters: [],
	flattenedOKRs: {},
	closedParents: {},
	error: ""
});
